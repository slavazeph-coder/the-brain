# Authorized BrainSNN production orchestration implementation

User explicitly requests full architecture wired into brainsnn.com: persistent render queue, exclusive GPU scheduling (video priority; inference may pause), CrewAI bounded monetization research/drafting workers via existing vLLM, authenticated website operations UI, live job/checkpoint status, approval gates, recovery and kill switch. Implement working code, tests and runbook, not mocks/stubs. Do not claim 24/7 or 99% reliability from a test.

First inspect current source, project conventions and deployment definitions. Project memory contains obsolete OpenClaw references: do NOT read archives/credentials or restore OpenClaw. Current production repo is this clone. Keep existing homepage and analysis contract intact. Reuse existing outbound worker transport where appropriate, one canonical scheduler. No competing watchdog.

Scope in this pass: build and locally test complete vertical slice in a feature branch; do NOT deploy, push, change live remote processes, access production secrets, or invoke real paid APIs. Do not alter shared .ai-memory. Record report in IMPLEMENTATION_RESULT.md, with actual test output and any blockers. Network/package installs allowed within project environment; never disable approval/sandbox.

Requirements: durable job store (inspect existing DB; no memory-only queue); leases with fencing and expiry; idempotent submissions; video priority at job boundary, no inference/render GPU overlap; stage checkpoints and resume; bounded retries for transport faults, no retries for NVML/CUDA/Xid hardware loss until explicit clearance; immutable artifact references; validate owner auth and worker credentials with fail-closed missing configuration. Private ops UI, public controls forbidden. Monitored state must distinguish queued, generating, decoding, ready-for-review, failed, paused. A rendered file is NOT visually approved or a sale. Separate approval records for outreach/publication/spend; external execution disabled by default and cannot be enabled by agent prompt.

CrewAI must be real, version pinned and compatible with existing Python runtime or isolated venv; bounded worker count and iterations, no arbitrary shell/network tools, no recursive unbounded agents. Explicit evidence-backed output contracts; pending human approval, zero external spend. vLLM existing OpenAI API route, no fake successful agent results when unavailable. Avoid treating abliterated model as an authority.

Known current operations context (do not mutate): GPU RTX4090 in container, existing supervisor ops/gpu/runtime.py serving BrainSNN. Host Xid79 PCIe disconnects previously occurred, user reports host clearance but requires safe stops on recurrence. Six clips saved, segment06 CPU decode currently running through a local Hermes watcher. Existing ComfyUI v0.3.65 GPU server remote loopback8190, CPU8189. Closing SSH previously left server running with a broken stderr pipe; current renderer redirects logs to remote file. Do not stop it or compete with it. Preserve /workspace/slava and all models/artifacts. Deployment needs coordination with existing Codex recovery monitor and durable container startup; document exact safe cutover/rollback rather than assuming permissions or process ownership.

Use RED/GREEN tests on scheduler/auth/approval invariants, run relevant existing suites and frontend production build. Provide actual end-to-end local execution using controlled test adapters clearly labeled, and separately identify live model/render validation still needed. If any prerequisite is unavailable, report accurately. Do not use fabricated production proof.
