# GT3 publication and provider evidence, 2026-09-20

The user's final preference is BrainSNN, not XIO.ca. Canonical route is
https://www.brainsnn.com/sponsor/gt3/. Preserve the existing robot sponsor studio
at /sponsor/, the BrainSNN homepage and the separate /lab product.

The clean GT3 build uses one stylesheet, one page controller, a small financial
module and the attributed official Sketchfab bridge. Same-site illustrative
poster; optional real model; Simple/Advanced controls; private SQLite proposals
with signed session/CSRF, validation, idempotency and owner access. Payments stay
disabled. No purchased vehicle, sponsors, bids or reach is represented as proven.

Actual GitHub Actions provider diagnostics (run 35533991612) fetched both SDK
versions 1.12.1 and 1.12.0 with HTTP 200, and fetched the exact Porsche embed.
Both returned api.ready error: No Hardware Support for Webgl. The standard
provider view changed after dragging but rendered the 360-degree image fallback,
not a confirmed WebGL scene. Do not cite a rotating image or green CI as proof
that logo decals or real GPU rendering worked. Compatible physical-device
rendering remains a separate acceptance check. The UI now discloses that
limitation and offers up to three fresh connection attempts without losing the
proposal form. It never labels iframe-load as scene-ready.

GT3 lab verification separates mocked provider contracts and actual isolated
SQLite submissions from external provider evidence. GT3 live acceptance runs
read-only GET/browser checks after the deployment workflow, rather than holding
the deployment's own CI gate open. It does not send live leads or payments.
