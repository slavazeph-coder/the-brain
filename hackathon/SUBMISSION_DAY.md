# Submission Day Playbook — May 18 → 19, 2026

Linear playbook for the final two days. Times are PT (San Jose
local; demo day is at McEnery Convention Center even though we're
submitting online).

## May 18 (Saturday) — Build Day

### 09:00 — Final pull + smoke

```bash
cd /Users/slavaz/the-brain
git checkout hackathon-techex
git pull --rebase origin hackathon-techex
git log --oneline -10        # verify Codex's intent classifier landed
```

Verify:

- `brainsnn-r3f-app/src/utils/firewallIntent.js` exists, was added
  by Codex's commit
- `brainsnn-r3f-app/src/utils/firewallIntent.test.js` exists
- `hackathon/cache/intent-scores.json` exists (production cache,
  generated by precompute-intent.mjs)
- `git push --dry-run origin hackathon-techex` reports nothing to
  push (i.e., we're synced)

Then:

```bash
cd brainsnn-r3f-app && npm install && npm test           # full unit run
npm run build                                            # production build
cd ../ui/brainsnn-viewer && npm install && npm run build
cd ../brainsnn-site && npm install && npm run build
```

All four must pass. If anything red, fix in the Claude lane and push;
ping Codex if it's a `firewallIntent.js` issue.

### 10:00 — Live URL smoke

```bash
curl -sI https://brainsnn.com | head -3
# expect: HTTP/2 200, server: railway-edge
```

Open Chrome → brainsnn.com → click through DEMO_SCRIPT beats 1, 2,
3, 4, 5 in real time. Document anything that's broken in
`hackathon/VERIFICATION.md` (append a 2026-05-18 section).

### 12:00 — Lunch + buffer

If anything's broken, this is when you fix it. Reserve at least 2
hours of buffer.

### 14:00 — Recording

Follow [RECORDING_PREP.md](RECORDING_PREP.md) end-to-end. Plan for 3
takes; pick the best one.

### 17:00 — Post-process

Trim, add lower-thirds, export H.264 1080p ≤ 100 MB. Save to:

- `~/Desktop/brainsnn-techex-final.mp4` (working copy)
- Upload to YouTube as unlisted (backup hosting in case lablab.ai
  uploader is flaky)
- Upload to Drive / Dropbox (third backup)

### 19:00 — Per-track write-up review

Open each `submissions/*.md` and:

- Replace the `[MP4 link to be inserted post-recording]` placeholder
  with the actual lablab.ai or YouTube URL
- Verify all relative paths in the doc resolve on GitHub
- Re-read for typos / broken sentences (you've been staring at this
  for a week, ask Codex or a friend to skim)

### 21:00 — Pre-submission checks

- [ ] `hackathon-techex` branch is publicly accessible on GitHub
      (the repo is private; either flip to public for hackathon or
      add a judge-level read collaborator with a documented account)
- [ ] All artifact links in submissions resolve to the
      `hackathon-techex` branch, not `main`
- [ ] Video plays cleanly start to finish in a clean Chrome profile
- [ ] brainsnn.com loads cleanly in a clean Chrome profile (no
      cached state)

## May 19 (Sunday) — Submit Day

### 08:00 — Wake + final sanity

- Re-run the curl smoke against brainsnn.com
- Re-play the recorded MP4 once
- Verify lablab.ai is accepting submissions for all 5 tracks

### 09:00 — Submission window opens

For each of the 5 tracks, submit via the lablab.ai web form:

| Track                        | Submission file                                        | Demo MP4 timestamp        |
| ---------------------------- | ------------------------------------------------------ | ------------------------- |
| Enterprise Security          | [submissions/security.md](submissions/security.md)     | 0:18 – 1:30 + 2:30 – 3:15 |
| Physical AI & Robotics       | [submissions/physical.md](submissions/physical.md)     | 1:30 – 2:30               |
| Agentic AI Workflows         | [submissions/agentic.md](submissions/agentic.md)       | 2:30 – 3:15               |
| Enterprise Data Intelligence | [submissions/intel.md](submissions/intel.md)           | 3:15 – 4:15               |
| Enterprise Problem-Solving   | [submissions/enterprise.md](submissions/enterprise.md) | 4:15 – 4:45 + 0:18 – 1:30 |

Each submission needs:

- **Title**: from the submission .md hero line
- **Tagline**: the "Hero claim" sentence
- **Description**: paste the rest of the .md (≤ 500 words usually
  fits the lablab.ai field; if not, paste the first half + a link
  to the full doc on GitHub)
- **Demo video URL**: the YouTube unlisted link (use lablab.ai's
  upload only if their UI is happy; otherwise YouTube is more
  reliable)
- **GitHub URL**: `https://github.com/slavazeph-coder/the-brain/tree/hackathon-techex`
- **Live URL**: `https://brainsnn.com`
- **Track tag**: pick the right track from the dropdown

After each submission, screenshot the confirmation page and save
to `~/Desktop/lablab-confirmations/`.

### 12:00 — Discord watch begins

Join the lablab.ai Discord. Pin the #techex-questions channel.
Judges typically Q&A directly through Sunday afternoon.

Be ready to:

- Answer questions about the architecture (point at INTEGRATION.md)
- Re-share the live URL if it scrolled out of chat
- Show specific layers if a judge asks "where is X?"

### 15:00 — Demos awarded live (per lablab.ai schedule)

Watch the live announcement stream (TechEx provides one). Don't
expect to win every track — the goal was a strong showing across
all 5, not a single win.

### 17:00 — Post-submission

If we placed in any track, draft a thank-you / next-steps note for
the lablab.ai judges and the BrainSNN community.

If we didn't place, that's fine — the artifacts are durable and the
submissions are the seed for ongoing BrainSNN positioning. The
recorded MP4 is reusable for product marketing.

### 19:00 — Retrospective

Update `the-brain/.ai-memory/MEMORY.md` under the
"TechEx Hackathon — Started 2026-05-06" section:

- What landed in the actual submissions
- Which track placement (if any)
- Anything we'd do differently
- Pull request to merge `hackathon-techex` → `main` if any of the
  hackathon work belongs in the canonical product (very likely:
  firewallIntent.js, the corpus samples, the persona-aware scoring
  improvements)

## Risk register for submission day

| Risk                                            | Mitigation                                                                                                                                    |
| ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| brainsnn.com Railway service is down            | Fall back to localhost demo path documented in [RECORDING_PREP.md](RECORDING_PREP.md); recorded MP4 still works                               |
| GitHub repo is still private                    | Flip to public 24h ahead of submission; add a judge collaborator if you must keep it private                                                  |
| MP4 fails to upload to lablab.ai                | YouTube unlisted is the canonical backup; reference the YouTube URL in the description, attach the file too                                   |
| Codex's firewallIntent.js never lands           | Use the cuts-and-alts in [DEMO_SCRIPT.md](DEMO_SCRIPT.md); record the deterministic-only version of each beat                                 |
| You catch COVID / something else / can't submit | Codex has all the artifacts on the branch; he can submit all 5 tracks via the same lablab.ai forms                                            |
| A judge asks for a feature we don't have        | Honest answer is best: "Out of scope for this MP4 but the architecture supports it; here's where it would slot in [point at INTEGRATION.md]." |

## Final reminder

The branch `hackathon-techex` is the artifact, not the trophy. Even
if no track wins, every doc on the branch is reusable for ongoing
BrainSNN positioning. INTEGRATION.md becomes the production spec for
firewallIntent.js. The corpus becomes the regression test set. The
scenarios become product marketing. The submissions become press
releases.

**Ship the work; the win is downstream.**
